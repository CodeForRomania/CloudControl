CloudControl UI and Backend
readme.md
install watchman file watcher
    git clone https://github.com/facebook/watchman.git
    cd watchman
    ./autogen.sh && ./configure && make
    sudo make install
#is it installed?
    which -a watchman
